# Example environment variables for the Instagram posting tool
# Copy to `.env` and fill values before running.
ACCESS_TOKEN=
IG_USER_ID=
API_VERSION=v17.0
# If you need a different Graph host, set GRAPH_HOST
GRAPH_HOST=graph.instagram.com
# Firebase / Google Cloud Storage settings for local-file uploads
# Point to your service account JSON, or set GOOGLE_APPLICATION_CREDENTIALS in your environment
GOOGLE_APPLICATION_CREDENTIALS=
# Your Firebase Storage bucket, e.g. "your-project-id.appspot.com"
FIREBASE_STORAGE_BUCKET=
